name: Build

on:
  push:
  pull_request:

permissions:
  contents: write

jobs:
  setup:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup environment
        uses: ./.github/actions/setup-ci

  test:
    needs: setup
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup environment
        uses: ./.github/actions/setup-ci

      - name: Test
        run: make test

  build:
    needs: test
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup environment
        uses: ./.github/actions/setup-ci

      - name: Build
        run: make build

      - name: Check manifest version for release tags
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        run: bash scripts/ci-check-manifest-version.sh "${{ github.ref_name }}" assets/manifest.json

      - name: Pack
        run: make pack

      - name: Upload plugin artifact
        uses: actions/upload-artifact@v4
        with:
          name: streamdeck-plugin
          path: dist/com.valentderah.just-monitor-control.streamDeckPlugin
          if-no-files-found: error
          retention-days: 7

      - name: Publish GitHub Release
        if: github.event_name == 'push' && !github.event.repository.fork && startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          TAG="${{ github.ref_name }}"
          if [[ ! "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Not a three-part release tag ($TAG), skip release"
            exit 0
          fi
          SEMVER="${TAG#v}"
          ASSET="dist/com.valentderah.just-monitor-control-${SEMVER}.streamDeckPlugin"
          cp dist/com.valentderah.just-monitor-control.streamDeckPlugin "$ASSET"
          NOTES="$(git tag -l --format='%(contents)' "$TAG" | sed '/^-----BEGIN PGP SIGNATURE-----$/,$d' | sed -e :a -e '/^\n*$/{$d;N;ba' -e '}')"
          if [[ -z "${NOTES// }" ]]; then
            NOTES="Plugin ${TAG}"
          fi
          gh release create "$TAG" "$ASSET" --title "$TAG" --notes "$NOTES" --verify-tag
